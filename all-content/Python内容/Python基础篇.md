#### 基础篇



`1.input() 和 print()`
```
例： name = input('请输入你的名字')
     print(name)
     
(注意：input括号中需要加引号，而且输入的为字符串类型！)
```

`2.if else条件语句`

```
例： a = 100
    if a >= 0:
      print(a)
    else:
      print(-a)
```